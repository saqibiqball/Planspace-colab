import React from "react";
import { render } from "@testing-library/react";
import ResetPassword from "./resetingPassword";
test("It Should Render Reset Password Component", () => {
    render(<ResetPassword />);
});
